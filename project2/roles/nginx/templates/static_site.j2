server {
        listen 80 default_server;
        listen [::]:80 default_server;
        server_name _;
        location /service_data {
                index service_state;
                alias /opt/service_state;
        }
}
